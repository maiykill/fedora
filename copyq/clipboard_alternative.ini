[Command]
Name=Clipboard Persistence (Minimal)
Automatic=true
MatchClipboard=owner_change
Command="
copyq:
var clip = str(clipboard())
if (clip && clip.length > 0 && clip != str(read(0))) {
    copy(clip)
}
"
Icon=\xf02e

